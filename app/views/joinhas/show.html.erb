<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @joinha.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @joinha.description %>
</p>

<p>
  <strong>Photo:</strong>
  <%= image_tag(@joinha.photo_url, width: 600) if @joinha.photo.present? %>
  
<h3>Comments</h3>
<% @comments.each do |comment| %>
  <div>
    <strong><%= comment.user_name %></strong>
    <br />
    <p><%= comment.body %></p>
    <p><%= link_to 'Delete', comment_path(comment), method: :delete, data: { confirm: 'Você tem certeza?' } %></p>
  </div>
<% end %>
<%= render 'comments/form' %>
</p>

<p>
  <strong>Upload date:</strong>
  <%= @joinha.upload_date %>
</p>

<%= link_to 'Edit', edit_joinha_path(@joinha) %> |
<%= link_to 'Back', joinhas_path %>
