<%= form_for(@joinha) do |f| %>
  <% if @joinha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@joinha.errors.count, "error") %> prohibited this joinha from being saved:</h2>

      <ul>
      <% @joinha.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.file_field :photo %>
  </div>
  <div class="field">
    <%= f.label :upload_date %><br>
    <%= f.date_select :upload_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
